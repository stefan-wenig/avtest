version: 1.0.{build}
image: Visual Studio 2017

before_build:
 - dotnet restore

build_script:
 - dotnet build /verbosity:quiet "avtest.sln"

artifacts:
- path: HelloWorld\bin\$(configuration)\netcoreapp2.0\HelloWorld.*
  name: HelloWorld
