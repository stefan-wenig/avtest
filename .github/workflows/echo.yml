name: echo
on: push
env: 
  wflevel: "value"
jobs:
  build:
    outputs: 
      arg1: "val"
    env:
      joblevel: "value"
    runs-on: ubuntu-latest
    steps:
      - id: me
        run: |
          echo "${{vars.ECHOTEXT}}"
          echo '${{ toJson(jobs.build.outputs) }}'
        env: 
          steplevel: "value"
      
