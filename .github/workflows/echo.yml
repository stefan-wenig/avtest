name: echo
on: push
env: 
  wflevel: "value"
jobs:
  build:
    env:
      joblevel: "value"
    runs-on: ubuntu-latest
    steps:
      - id: me
        run: |
          echo "${{vars.ECHOTEXT}}"
          echo '${{ toJson(jobs.env) }}'
          echo '${{ toJson(env) }}'
        env: 
          steplevel: "value"
      
